# meetrail-site (ミトレル 公式サイト)

スマホアプリ **ミトレル (Meetrail)** の公式サイト。ビルド不要の素の HTML を GitHub Pages で配信する。

- 公開 URL: https://sekai-nina.github.io/meetrail-site/
- アプリ本体: `../meet-greet-app` (別リポジトリ [sekai-nina/meet-greet-app](https://github.com/sekai-nina/meet-greet-app))
- 構成・デプロイ手順は [README.md](README.md) を参照

## 行動原理

1. **書く内容はアプリの実装と照合する** — 機能説明・データの取扱いを書くときは、記憶や推測でなく `../meet-greet-app` の実コードを確認してから書く。特に法務ページは事実と食い違うと審査・信頼の両方に響く
2. **UI テキストは日本語** — サイト上の表示はすべて日本語
3. **相対パスで書く** — サブパス配下 (`/meetrail-site/`) で配信されるため、`/assets/style.css` のような絶対パスは壊れる。必ず `assets/style.css` と書く
4. **非公式である旨を全ページに残す** — フッターの注記は消さない。「公式アプリと誤認させない」は利用規約第1条でも約束している内容

## やらないこと

- **ビルドツール・フレームワークを導入しない** — 素の HTML + 1 枚の CSS で完結させる方針。npm も CI も持たない
- **外部スクリプト・アクセス解析・広告を入れない** — プライバシーポリシー第1項で「解析ツールを設置していない」と明言している。追加するならポリシーの改定が先
- **アプリの実装より先にサイトの記述を進めない** — 未実装の機能を「できます」と書かない
- **混雑表・完売表の算出ロジックを具体的に書かない** — 仕様を大きく変更する予定があるため、「利用者の記録から算出した目安」以上に踏み込まない (閾値・計算式・指標名を書くと、変更のたびにサイトが嘘になる)
- **メンバーの画像・楽曲のジャケット等を掲載しない** — 権利の問題。掲載してよいのは自作のアプリ画面のみ

## サイト構成上の約束ごと

### ヘッダー / フッターは各 HTML に複製されている

テンプレートエンジンを持たないため、`<header class="site-header">` と `<footer class="site-footer">` は
全 8 ページに同じ内容がコピーされている。**変更するときは全ファイルを一括で直す**。

```bash
grep -l "site-footer__disclaimer" *.html   # 対象ファイルの確認
```

現在ナビに出しているのは 4 ページ (ホーム / 使い方 / よくある質問 / お問い合わせ)。
法務系 4 ページはフッターからのみ辿れる。ページを増やしたらフッターのリンクにも足すこと。

現在のページで開いているものには `aria-current="page"` を付ける (下線が付く)。

### 色は必ず CSS 変数を使う

`assets/style.css` の `:root` にトークンを定義してある。HTML 側にも CSS 側にも **HEX 直書きをしない**。

トークンの値は**アプリ側 (`../meet-greet-app` の `tailwind.config.js` / `lib/theme-colors.ts`) と
二重管理**になっている。ブランド色を変えるときは 3 ファイルすべてを直す。

### 差し替え前提の箇所には HTML コメントを残す

未リリースゆえの仮の表示 (CTA ボタン、スクショ枠) には、何にどう差し替えるかを HTML コメントで書いてある。
同種の仮置きを足すときも同じようにコメントを残す。

## 法務ページを触るときのルール

`privacy.html` / `terms.html` / `delete-account.html` は審査と信頼に直結する。

- **事実と異なることを書かない。** アプリが取得している情報・使っている外部サービスは、
  `../meet-greet-app` の `package.json` (SDK)、`app.json` (権限)、`supabase/migrations/` (保存項目) で確認する
- **アプリの挙動を変えたら、法務ページも同じ PR の範囲で見直す。** 特に次を変えたときは必ず読み返す:
  - 認証方法の追加・削除 (現在: メール / Apple / Google / 匿名)
  - 外部 SDK の追加 (解析・広告・クラッシュレポート等を入れたら要改定)
  - 端末権限の追加 (現在: 写真の**書き込み**のみ)
  - 他ユーザーに何かを見せる機能の追加・変更
- **本文を実質的に変更したら、ページ上部の日付を更新する** (`<p class="doc__meta">制定日: ...</p>`)。
  軽微な誤字修正では変えなくてよい
- **「レポ・メモは本人専用」「集計は匿名」は現在の実装上の事実。** ここを変える実装をするなら、
  サイトの記述変更ではなく、まず設計判断としてユーザーに確認する

## 現時点の事実 (2026-07 時点)

サイトの記述の根拠。アプリ側が変わったら、ここも一緒に直す。

- アプリはまだ**未リリース** (App Store 申請準備中)。CTA は非活性の「近日公開」表示
- 認証は メール OTP / Apple / Google / 匿名ログイン の 4 種
- データは Supabase (東京リージョン) に保存。行レベルのアクセス制御で本人のみ参照可
- **レポ・メモ・非公開メモ・話題計画の本文は本人専用**。他ユーザーへの公開機能は持たない
- **当選等の数値の記録は匿名集計され、他ユーザーの混雑の目安表示に使われる**
- 広告 SDK・アクセス解析ツールは未導入
- 写真は**保存のみ** (レポ画像の書き出し)。カメラロールの読み取りはしない
- アカウント削除はアプリ内から可能で、削除時に全データが連鎖削除される

## 運営者情報 (全ページ共通)

- 運営者: **世界新奈**
- 連絡先: **sekainina.project@gmail.com**
- 著作権表記: `© 2026 世界新奈`

## コミットルール

アプリ本体と同じ絵文字プレフィックスを使う。

| 絵文字 | 用途 |
|--------|------|
| `:dog2:` | ページ・コンテンツの追加 |
| `:fish:` | 表示崩れ・リンク切れの修正 |
| `:balloon:` | 整理・リファクタリング |
| `:icecream:` | 文言・ドキュメントの更新 |
| `:art:` | デザイン・スタイリング |
| `:bubbles:` | 設定・環境変更 |

## 確認方法

```bash
python3 -m http.server 8000   # → http://localhost:8000/
```

変更後は最低限これを確認する。

- [ ] 全ページのヘッダー / フッターのリンクが切れていない
- [ ] スマホ幅 (375px 相当) で横スクロールが出ない
- [ ] 相対パスで書かれている (`href="/..."` になっていない)
- [ ] 非公式の注記がフッターに残っている
